# Phase-log — parsing & KG simplification

Companion document to `2026-06-06-parsing-simplification.md`. Each phase
records (a) the verification gate's actual output, (b) any deviations
from the audit's predicted numbers, (c) deferred follow-ups, (d) any
subagent reports the coordinator escalated as borderline.

Settings the user chose at plan kickoff (2026-06-06):

- **Workspace isolation:** single WIP commit (`19d3e15 wip: in-progress
  parsing/KG changes pre-simplification`). The plan's phase commits land
  on top of that.
- **Gate strictness:** stop only on hard failures; log borderline
  findings here for review.

---

## Phase 0 — preflight (2026-06-06)

### KG baseline (live `data/knowledge.db`, ~95.5 MB)

```
NODES:
  agent                  5415
  source_text            3625
  institution            3931
  concept                8455
  term                  16798
  translation_mapping   54152

EDGES:
  has_mapping           54152
  maps_to               54152
  translates_to        102180
  instantiates_concept  46897
  instantiated_in       64111
  attributed_to         56925
  written_by             2672
  cited_in               1292
  translated_by           185
  performed_by             56
  published_by            575
  sl_published_by          20
  hosted_by                84

concepts_with_definition: 0
lineage_edges (extends/critiques/redefines/reappropriates/related_to): 0
```

These numbers **exactly match** the audit's claimed baseline. Phase 0
verification gate PASS.

### Snapshot

- `data/knowledge.db.preflight.bak` (95.5 MB) — byte-for-byte copy of
  the live KG at preflight.

### Discoveries

- `KnowledgeGraph.__init__` does NOT take `load_from_file`. Correct
  invocation is `KnowledgeGraph()` (loads from default `db_path`). Plan
  text corrected in-place.
- Project venv is `.venv/bin/python3` (no `python` symlink). Plan text
  corrected globally.
- `data/` and `docs/` are in `.gitignore`. The plan's commit steps
  updated to (a) not commit `data/knowledge.db`, (b) force-add curator
  INPUT data files (`data/segment_title_attribution.json`,
  `data/lineage_schools.json`, `data/concept_theorists.json`), (c)
  force-add audit + plan + this phase-log doc.

### Out-of-scope confirmation

`import_book.py` confirmed as the only editor-doc-prep consumer of
`vl_parser.py` symbols (`split_paragraphs` at `:112`, `BookOutline` at
`:147`). This locks Phase 2 as a prereq to Phase 7's `vl_parser.py`
deletion.

### Coordinator note

Phase 0 is complete.

### Advisor review (between Phase 0 and Phase 1 dispatch)

Three points came back; all addressed in-place in the plan:

1. **Blocker mitigated (Phase 1 design change).** Original plan said
   "sort `self.entries` chronologically." Advisor flagged: editor surface
   `main.py:246-251` and an inline test read `tm.entries` (one
   positionally). Greps confirmed:
   - `main.py:246-249` iterates by content (`for entry in tm.entries:
     if entry.get("source") == ...`) — order-agnostic. ✓
   - `main.py:251` `tm.entries.append(...)` — appending at the end
     works regardless of order. ✓
   - `inline_test/test_confirm_pipeline.py:94` reads `tm.entries[0]`
     positionally — bounded blast radius (a fixture).
   - All other `tm.entries` consumers iterate or filter by `origin`.
   Even so, adopted advisor's safer design: keep `self.entries` in
   natural load order; attach `t_index`/`raw_index`/`creationdate` to
   each entry; expose `iter_chronological()` as a separate view.
   Phase 1 spec + tests + Step 1.4 verification rewritten accordingly.
2. **Phase 3 gate tightened.** Added hard-failure threshold: review-
   routing share > 80% halts the phase and surfaces to user.
3. **Phase 12 caveat added.** Existing smol payload lacks `t_index`;
   chose to record Option A vs B decision in this phase log when
   Phase 12 arrives.

Next: commit the plan revisions, then dispatch the Phase 1 TDD-red
subagent.
