# Phase-log — parsing & KG simplification

Companion document to `2026-06-06-parsing-simplification.md`. Each phase
records (a) the verification gate's actual output, (b) any deviations
from the audit's predicted numbers, (c) deferred follow-ups, (d) any
subagent reports the coordinator escalated as borderline.

Settings the user chose at plan kickoff (2026-06-06):

- **Workspace isolation:** single WIP commit (`19d3e15 wip: in-progress
  parsing/KG changes pre-simplification`). The plan's phase commits land
  on top of that.
- **Gate strictness:** stop only on hard failures; log borderline
  findings here for review.

---

## Phase 0 — preflight (2026-06-06)

### KG baseline (live `data/knowledge.db`, ~95.5 MB)

```
NODES:
  agent                  5415
  source_text            3625
  institution            3931
  concept                8455
  term                  16798
  translation_mapping   54152

EDGES:
  has_mapping           54152
  maps_to               54152
  translates_to        102180
  instantiates_concept  46897
  instantiated_in       64111
  attributed_to         56925
  written_by             2672
  cited_in               1292
  translated_by           185
  performed_by             56
  published_by            575
  sl_published_by          20
  hosted_by                84

concepts_with_definition: 0
lineage_edges (extends/critiques/redefines/reappropriates/related_to): 0
```

These numbers **exactly match** the audit's claimed baseline. Phase 0
verification gate PASS.

### Snapshot

- `data/knowledge.db.preflight.bak` (95.5 MB) — byte-for-byte copy of
  the live KG at preflight.

### Discoveries

- `KnowledgeGraph.__init__` does NOT take `load_from_file`. Correct
  invocation is `KnowledgeGraph()` (loads from default `db_path`). Plan
  text corrected in-place.
- Project venv is `.venv/bin/python3` (no `python` symlink). Plan text
  corrected globally.
- `data/` and `docs/` are in `.gitignore`. The plan's commit steps
  updated to (a) not commit `data/knowledge.db`, (b) force-add curator
  INPUT data files (`data/segment_title_attribution.json`,
  `data/lineage_schools.json`, `data/concept_theorists.json`), (c)
  force-add audit + plan + this phase-log doc.

### Out-of-scope confirmation

`import_book.py` confirmed as the only editor-doc-prep consumer of
`vl_parser.py` symbols (`split_paragraphs` at `:112`, `BookOutline` at
`:147`). This locks Phase 2 as a prereq to Phase 7's `vl_parser.py`
deletion.

### Coordinator note

Phase 0 is complete.

### Advisor review (between Phase 0 and Phase 1 dispatch)

Three points came back; all addressed in-place in the plan:

1. **Blocker mitigated (Phase 1 design change).** Original plan said
   "sort `self.entries` chronologically." Advisor flagged: editor surface
   `main.py:246-251` and an inline test read `tm.entries` (one
   positionally). Greps confirmed:
   - `main.py:246-249` iterates by content (`for entry in tm.entries:
     if entry.get("source") == ...`) — order-agnostic. ✓
   - `main.py:251` `tm.entries.append(...)` — appending at the end
     works regardless of order. ✓
   - `inline_test/test_confirm_pipeline.py:94` reads `tm.entries[0]`
     positionally — bounded blast radius (a fixture).
   - All other `tm.entries` consumers iterate or filter by `origin`.
   Even so, adopted advisor's safer design: keep `self.entries` in
   natural load order; attach `t_index`/`raw_index`/`creationdate` to
   each entry; expose `iter_chronological()` as a separate view.
   Phase 1 spec + tests + Step 1.4 verification rewritten accordingly.
2. **Phase 3 gate tightened.** Added hard-failure threshold: review-
   routing share > 80% halts the phase and surfaces to user.
3. **Phase 12 caveat added.** Existing smol payload lacks `t_index`;
   chose to record Option A vs B decision in this phase log when
   Phase 12 arrives.

Next: commit the plan revisions, then dispatch the Phase 1 TDD-red
subagent.

---

## Phase 1 — TMX chronological ordering (2026-06-06)

### TDD-red

Fixture `tests/fixtures/tmx_unordered.tmx` (4 TUs: natural order
[A,B,C,D] with creationdates [2024, 2022, 2023, None] → expected
t_indexes [2, 0, 1, 3]) and 6 failing tests in
`tests/test_tm_timecodes.py` covering: function exists, returns
natural-order list, all six keys present, t_index reflects
chronological order, dateless entries trail with t_index after dated
ones, `iter_chronological` yields t_index-sorted stream + filters by
origin, `self.entries` stays in natural order. All failures cited
missing symbols (ModuleNotFoundError / AttributeError / KeyError) — no
fixture-loading noise.

### TDD-green

New `translate_core/tm_timecodes.py` parses TMX via `lxml.etree`
(translate-toolkit's `tmxfile` silently drops the `creationdate`
attribute — the root cause of the §10.1 bug). Detects source/target
via per-`<tuv>` `xml:lang` rather than positional swap. Modified
`translate_core/tm.py`: `_load_tmx` delegates to the new reader and
assigns globally-unique `raw_index`; `_load_all` epilogue computes
global `t_index` after all files load so chronological ordering is
cross-origin meaningful. New `iter_chronological(origin=None)` method.
`lookup_fuzzy`, `search_concordance`, `search_prefix` are byte-
identical (verified).

### Verification gate — PASS

- New tests: 6/6 pass.
- Full test suite (excluding the documented pre-existing broken test
  in `tests/test_vl_parser.py` per `AGENTS.md:135`): **227 passed**.
- Real-data load against `data/tm/*.tmx` (5 files): **40,251 entries**.
  `self.entries` is in contiguous natural order (`raw_index == 0..40250`).
  `iter_chronological()` yields strictly ascending t_index.
  Four origins show chronological ≠ natural:
  `2022-SL-EN.tmx`, `big-EN-SL.tmx`, `mglc-EN-SL.tmx`, `MGLC-SL-EN.tmx`.
  Proves the timecode reader works on real TMXs.
- Editor read APIs intact: `lookup_fuzzy('art', threshold=70.0)` returned
  empty (as expected — token too short for a 70 % ratio against
  full-sentence sources, not a regression); against a real segment it
  scored 100.0. `search_concordance` returns 3 hits; `search_prefix`
  returns 11. All confirm the read APIs work.

### Pyright diagnostics (benign)

- `tm_timecodes.py:22` — `etree unknown import symbol`. lxml ships
  stubs but Pyright in this project isn't configured to find them.
  Runtime is fine (6/6 tests + 40,251-entry real load prove it).
- `tm.py:43` — `Import translate_core.tm_timecodes could not be
  resolved`. Pyright local-cache lag; module exists, runtime resolves.
  Not blocking.

### Commit

`b723134` — `phase1: TMX chronological t_index alongside natural raw_index`

### Next

Phase 2 — extract `BookOutline` + `split_paragraphs` from
`vl_parser.py` into a new editor-safe module so Phase 7 can delete
`vl_parser.py` without breaking `import_book.py`.

---

## Phase 2 — Extract BookOutline + split_paragraphs (2026-06-06)

### Outcome

New self-contained module `translate_core/book_outline.py` owns three
symbols (verbatim copies from `vl_parser.py`):
- `TOCEntry` dataclass (was `vl_parser.py:108-117`) — `BookOutline.entries`
  is `list[TOCEntry]`, so this had to come along.
- `BookOutline` dataclass (was `vl_parser.py:136-142`).
- `split_paragraphs(text, max_chars=1500)` (was `vl_parser.py:554-594`)
  plus its private helper `_flush_buffer` and the three module-level
  regex constants it depends on.

Only `re` + `dataclasses` from stdlib are imported — nothing from
`vl_parser`. The new module is therefore stable when Phase 7 deletes
`vl_parser.py`.

`import_book.py` updated at exactly two lines (112 and 147); no other
lines touched. `vl_parser.py` diff is empty (confirmed). The duplicate
definitions in `vl_parser.py` are dead-code-walking from Phase 2 onward.

### Verification gate — PASS

- New module + editor consumer both import cleanly.
- `vl_parser.py` still imports its copy of `BookOutline` / `split_paragraphs`
  (still in place; deleted in Phase 7).
- New smoke tests: **4 passed** (split chunks the source observes from
  `inspect.signature`-derived `max_chars`, empty/whitespace shortcuts,
  `BookOutline()` default fields, `TOCEntry.source_page == -1`).
- Full suite (excluding `tests/test_vl_parser.py` per `AGENTS.md:135`):
  **231 passed**.
- `git diff translate_core/vl_parser.py` is empty (file byte-identical).

### Pyright diagnostics (benign)

- `import_book.py:112,147` and `tests/test_book_outline.py:12`:
  `translate_core.book_outline could not be resolved`. Same Pyright
  local-cache lag as Phase 1's `tm_timecodes` import warning. Runtime
  resolves; tests pass.
- `import_book.py` "re/time/BookOutline not accessed" are pre-existing
  style notes unrelated to this phase.

### Commit

`cd121b3` — `phase2: extract BookOutline + split_paragraphs into self-contained module`

### Next

Phase 3 — confidence-bump fix (replace `+0.60 smol_verified_classification`
blanket bump with a 2-of-3 composite gate). Independent of Phase 2.

---

## Phase 3 — Confidence bump fix (2026-06-06)

### TDD-red

11 tests in `tests/test_confidence.py`. 10 fail against the current
implementation (universal `+0.60` bump still floors smol records at
DIRECT_WRITE; composite + curator_endorsed bumps absent). 1 thresholds
guard rail test (`THRESHOLD_DIRECT == 0.85`, `THRESHOLD_REVIEW == 0.55`)
passes — kept as protection against future silent threshold drift.

### TDD-green

`translate_core/entity_extraction/confidence.py` rewritten universal
bumps block (+31 / −13):
- REMOVED `+0.60 smol_verified_classification` (the flag stays as an
  informational signal; the bump is gone).
- KEPT `+0.15 smol_extracted`, `+0.10 verified_from_text`.
- ADDED `composite_2of3 +0.30` when ≥ 2 of
  `{title_bilingual, container_attached, project_type_typed}` truthy.
  `has_bilingual_title` aliases `title_bilingual` and counts at most
  once.
- ADDED `curator_endorsed +0.40` (independent of composite).
- ADDED `round(s, 6)` before tier classification (the previous +0.60
  bump masked IEEE 754 underflow: `0.30+0.15+0.10 = 0.5499999…` would
  miss the 0.55 REVIEW boundary by 1 ULP and drop to DROP).

`translate_core/entity_extraction/smol_extractor.py` updated
`_build_cited_work`, `_build_concept`, `_build_artwork`,
`_build_performance` signals dicts to emit the new composite signals.
`_build_agent_person` and `_build_institution` left unchanged (no
titles / containers / project_types in their domain — they earn
DIRECT via per-kind bumps).

### Verification gate — PASS

- All 11 Phase 3 tests pass.
- Full suite (excluding `tests/test_vl_parser.py` per `AGENTS.md:135`):
  **242 passed**, zero regressions.
- Real-data sweep (`data/smol_entities_map/smol_extractions.json`,
  26,607 records evaluated through the actual `_build_*` functions +
  `score_record`): **direct=46%, review=54%, drop=0%** — well under
  the 80% hard-failure threshold for review-share.

### Deviations from plan — user-approved

User confirmed all three:

1. **Per-kind `has_bilingual_title +0.10` removed from `cited_work`
   branch.** The plan said "per-kind branches unchanged" but keeping
   this would double-credit the bilingual axis already covered by the
   composite. Bilingual cited_works land at 0.85 via composite; with
   an explicit SL edition, +0.10 more brings them to 0.95.

### Deferred follow-ups (logged here, not Phase 3 scope)

2. **Institution scoring starves to 100% REVIEW.** Smol's
   `_build_institution` only sets `named_institution_kind`; the
   per-kind branch needs `known_publisher` / `multi_mention` /
   `institution_pattern_match` to clear 0.85. Every smol-extracted
   institution lands at 0.30+0.15+0.10+0.20 = 0.75 → REVIEW.
   Phase 12's end-to-end run will surface a ~8,800-institution
   review queue. Address in a follow-up (probably a tweak to the
   institution builder to emit `multi_mention` when seen across
   origins, and `known_publisher` against a curated whitelist).

3. **`VALID_CITED_PROJECT_TYPES` includes the fallback values
   `cited_work` and `cited_container`.** Per ontology §2.4.1 these
   are NOT typed values, they're fallbacks. Phase 3 worked around
   this by explicitly excluding both from the `project_type_typed`
   signal computation. Fix in Phase 5 (routing chokepoint), which is
   the natural place for typed/cited separation enforcement.

### Pyright diagnostics (benign)

Stale pre-existing notes on `smol_extractor.py` lines 46/47 (unused
`re`/`unicodedata` imports at module top) and lines 335/401/456/588
(unused `src_lang`/`tgt_lang` parameters). None relate to Phase 3.

### Commit

`7d2e5c2` — `phase3: drop +0.60 universal smol bump; composite + curator gates`

### Next

Phase 4 — COBISS bilingual title encoding (`title_orig` +
`title_translation` + `orig_lang` + `translation_lang` on container
nodes). Independent of Phase 3.

---

## Phase 1B — Language-neutrality remediation (2026-06-06)

### Why this phase exists

User pushed back on the "language neutrality" constraint I'd added to
the plan — pasting it at the top didn't audit the rest. A pythonista
reviewer was dispatched against the plan AND the Phase 1/3 committed
code; the audit (`docs/parsing_simplification_lang_neutrality_audit.md`)
found two hard runtime bugs:
1. `tm_timecodes.py:97-106` hardcoded `lang == "en"` / `lang == "sl"`
   pairing — non-EN/SL TMX would load as zero entries.
2. `smol_extractor._detect_source_lang` defaulted to `(LANG_EN,
   LANG_SL)` on no filename match — every unrecognised filename
   silently mislabelled as EN→SL.

### Execution flow (per agent-mix table)

1. **Explore** — impact survey (`docs/phase1b_impact_survey.md`).
   Confirmed the compat shim is sufficient: every current `tm.entries`
   consumer is order-agnostic or relies on EN→SL literals that the
   filtered view preserves.
2. **feature-dev:code-architect** — blueprint design
   (`docs/phase1b_blueprint.md`). 11 sections covering the
   `_entries_by_pair` index, the compat-shim derivation, the loader
   rewrite, the smol detector fix, the legacy-alias gating, and
   migration sequence with three named risks.
3. **python-pro** — TDD red. 17 new failing tests + 22 regression
   locks across 5 test files + 2 new TMX fixtures. Discriminators
   verified to fail for the right reasons.
4. **python-pro** — TDD green. Implementation per blueprint.
5. Coordinator type-fix pass. The green subagent dropped
   `build_record`'s `str` defaults but didn't update the `_build_*`
   helpers' signatures to accept `str | None`. Pyright caught it;
   runtime didn't (Python doesn't enforce annotations). Fixed
   inline.
6. **pythonista-reviewer** — independent diff review. PASS on all
   13 checks (Constraint 9 compliance, compat-shim correctness,
   Risk 1 ordering, evidence-based `<tuv>` pairing, regex detector,
   None-guard, builder gating, signature change + caller migration,
   rename completeness, inline_test regression, full suite,
   asymmetry TODO, vestigial param classification).

### Verification gate — PASS

- 39 new + 264 existing tests pass (full suite, excluding documented
  broken `tests/test_vl_parser.py`).
- Real-data load: 40,251 entries across pairs `[('sl','en'),
  ('en','sl')]`; compat view has all entries as `en→sl`; `raw_index`
  contiguous; `iter_chronological` yields all 40,251.
- `inline_test/test_confirm_pipeline.py`: 9 passing, 2 documented
  pre-existing failures (unrelated to 1B).
- `has_sl_edition` references remaining: only in test fixture (locks
  the rename) and 2 documentation comments.

### Files changed

- `translate_core/tm.py` (+98 / -14) — `_entries_by_pair` index,
  `_build_compat_entries` with EN/SL filter + SL→EN swap mechanic,
  `_reindex_t_index` over all pairs, `iter_chronological` reads
  `_entries_by_pair`.
- `translate_core/tm_timecodes.py` (+47 / -42) — evidence-based
  `<tuv>` pairing, `_read_srclang` returns `None` not `"en"`,
  entries carry actual `xml:lang` codes.
- `translate_core/entity_extraction/smol_extractor.py` (+54 / -25) —
  regex `_detect_source_lang` returning `(None, None)`, `??`
  placeholder in `format_extract_prompt`, `build_record` kw-only
  required args, three `_build_*` legacy-alias conditional gates,
  signal rename, helper signatures widened to `str | None`.
- `translate_core/entity_extraction/confidence.py` (+8 / -2) —
  signal rename + documentation comment.
- `run_entity_extraction.py` (+9 / -1) — passes detected pair into
  `build_record` (caller migration; this script was missed in the
  blueprint's caller inventory).
- New fixtures: `tests/fixtures/tmx_hr_sl.tmx`,
  `tests/fixtures/tmx_sl_en.tmx`.
- New tests: `tests/test_tm_pair_indexing.py` (7),
  `tests/test_smol_extractor_detect.py` (parametrised),
  `tests/test_smol_extractor_lang_neutral.py` (2).
- Extended: `tests/test_tm_timecodes.py` (+ 3 HR-SL tests),
  `tests/test_confidence.py` (+ 2 rename tests).

### COMPAT-SHIM and SUNSET tags

- `tm.py:112, 114, 122` — `# COMPAT-SHIM: audited exception to
  constraint 9; sunset in Phase 11`. Three sites: EN-SL pair-key
  filter, SL-EN pair-key filter, swap mechanic.
- `smol_extractor.py:548, 759, 884` — `# SUNSET: Phase 11`. Three
  sites: `_build_cited_work`, `_build_artwork`, `_build_performance`
  legacy-alias conditional blocks.

### Risks confirmed addressed

- Risk 1 (`_reindex_t_index` ordering): `_load_all` runs
  `_reindex_t_index` BEFORE `_build_compat_entries`. Swapped copies
  inherit `t_index` from the indexed originals.
- Risk 2 (`main.py:251` append asymmetry): `TODO` comment on
  `iter_chronological` at `tm.py:156-161` documents the divergence.
- Risk 3 (`format_extract_prompt` `.upper()` crash on `None`):
  `?` placeholder guard at `smol_extractor.py:213-216`.

### Pyright diagnostics (resolved + remaining)

- Resolved by coordinator type-fix: 10 `_build_*` helper signatures
  widened to `str | None`.
- Remaining (non-blocking): pre-existing "unused param" warnings on
  six `_build_*` helpers' `src_lang`/`tgt_lang` parameters. These
  were vestigial from Phase 3 and existed before Phase 1B. Recommended
  for cleanup in a separate pass.

### Commit

`d152a20` — `phase1b: language-neutral TM loader + smol detector; EN/SL compat shim`

## Phase 4 — Language-neutral KG (partial)

### Done (commits 1bc7709, f49441c, dee8bec)

- `ontology.md` revisions (§2.4.2 canonical encoding only; §3.2
  `translation_published_by`; §4 invariant 4 rewrite)
- `scripts/ingest_personal_bibliography.py` — kwarg rename per
  `belina_role` branch + bilingual publisher `: =` split TODO finished
- `translate_core/entity_extraction/smol_extractor.py` — three
  `# SUNSET: Phase 11` alias-write blocks deleted; builder remaps
  `slovenian_edition` → `translation_edition` with `language` field;
  new `# SUNSET: Phase 11` tag on prompt-level `slovenian_edition` key
- `translate_core/knowledge_graph.py` — `link_sl_published_by` →
  `link_translation_published_by`
- `translate_core/kg_ingest_entities.py` — reader updates to read
  only neutral fields
- `docs/AGENT_CONTEXT.md` (new) — compact context doc referenced by
  subagents

### Reverted (commit dee8bec)

Premature mid-plan migration items that were my hallucinations
(migration is end-of-plan, not Phase 4):

- `scripts/migrate_to_neutral_ontology.py` (deleted)
- `tests/test_neutral_ontology_migration.py` (deleted)
- `tests/test_validate_kg_neutral.py` (deleted)
- `scripts/validate_kg.py` legacy field/edge violation enforcement
  (reverted)

### Open follow-ups (do later, not blocking Phase 5+)

- **OF-1: `tests/test_link_sl_published_by.py`** (4 tests) reference
  the renamed factory method; rename file → `test_link_translation_published_by.py`,
  update assertions. Mechanical.
- **OF-2: `tests/test_kg_ingest_compliance.py::test_sl_published_by_edge_present`**
  references the renamed edge relation. Mechanical rename.
- **OF-3: `scripts/ingest_extra_containers.py`** (new file for
  containers outside COBISS) not yet implemented. Phase 4 deliverable
  per blueprint §3.
- **OF-4: Plan text** still asserts Phase 4 includes data migration.
  Constraint 7 and Phase 4 description need to be corrected to say
  migration is end-of-plan work (Phase 12 or a new dedicated phase).
- **OF-5: Reader migration for the 75 sites in `docs/phase4_reader_inventory.md`**
  not yet done. Per user direction: no backward-compat fallbacks.
  Existing nodes with legacy attributes will read as blank until
  end-of-plan migration; that cost is accepted.

### Commit

Multiple commits: `1bc7709`, `f49441c`, `dee8bec`

---

## Phase 5 — Single-chokepoint routing by provenance

### Done

- `_route_record(record, *, container_index=None) -> tuple[str, dict]`
  in `translate_core/kg_ingest_entities.py` per blueprint §1.
- Two frozenset constants as single source of truth:
  `CONTAINER_PROVENANCE = {"cobiss_personal", "curator_extra"}`,
  `VALID_PROVENANCE = CONTAINER_PROVENANCE | {"tm_smol", "doc_pair"}`.
- `write_to_kg` pass 1 (line 509+) and pass 3 (deferred_cited loop)
  route via the chokepoint before per-kind handlers.
- Producer provenance stamping:
  - `smol_extractor.build_record` stamps `"tm_smol"` on every record
  - `document_pair_pipeline._heuristic_record` stamps `"doc_pair"`
  - `ingest_personal_bibliography.py` already wrote `"cobiss_personal"`
    in Phase 4 as a node kwarg (not on record; OK because that script
    writes direct to KG, not through write_to_kg)
- `tests/test_kg_ingest_compliance.py::_rec()` fixture extended with
  `provenance` kwarg; existing tests updated to supply the right value.
- `tests/test_kg_ingest_routing.py` (new) — 12 tests covering all
  decision-table branches.

### Verification gate

- 12/12 new routing tests pass.
- 284 existing tests pass (excluding `tests/test_vl_parser.py` per
  `AGENTS.md:135` and `tests/test_link_sl_published_by.py` per OF-1).
- One pre-existing failure: `test_sl_published_by_edge_present` —
  OF-2, not introduced by Phase 5 (confirmed via stash + re-run).

### Open follow-ups

- **OF-6 (Phase 5):** plan Step 5.5 expects a `run_entity_extraction.py
  --inspect` dry-run gate against the live KG. The `--inspect` flag
  doesn't exist yet on that script. Deferred — coordinator can add
  the flag in a small follow-up dispatch, OR accept the test-pass +
  reviewer-pass gate.

### Commit

`f0a4763` — `phase5: single-chokepoint routing by provenance`

### Next

Phase 5 Step 5.4 — pythonista-reviewer pass over the Phase 5 diff.
Then Phase 6 — chronological-anchor container attribution.
